# Merged System

`apps/orchestrator` remains the single control plane. `apps/dashboard` is the primary UI, `apps/socket-server` routes live traffic, `apps/blink-bridge` mirrors canonical task chat, and shared packages hold reusable contracts and services.
