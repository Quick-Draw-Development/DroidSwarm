export function BranchPolicyCard() {
  return (
    <section>
      <h3>Git Flow Policy</h3>
      <p>`feature/*` and `release/*` branch from `develop`; `hotfix/*` and `support/*` branch from `main`.</p>
    </section>
  );
}
